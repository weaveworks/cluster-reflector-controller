---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: automatedclusterdiscoveries.clusters.weave.works
spec:
  group: clusters.weave.works
  names:
    kind: AutomatedClusterDiscovery
    listKind: AutomatedClusterDiscoveryList
    plural: automatedclusterdiscoveries
    singular: automatedclusterdiscovery
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutomatedClusterDiscovery is the Schema for the automatedclusterdiscoveries
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AutomatedClusterDiscoverySpec defines the desired state of
              AutomatedClusterDiscovery
            properties:
              aks:
                description: AKS defines the desired state of AKS
                properties:
                  exclude:
                    description: Exclude is the list of clusters to exclude
                    items:
                      type: string
                    type: array
                  filter:
                    description: Filter criteria for AKS clusters
                    properties:
                      location:
                        description: Location is the location of the AKS clusters
                        type: string
                    type: object
                  subscriptionId:
                    description: SubscriptionID is the Azure subscription ID
                    type: string
                type: object
              name:
                description: Name is the name of the cluster
                type: string
              type:
                description: Type is the provider type
                type: string
            type: object
          status:
            description: AutomatedClusterDiscoveryStatus defines the observed state
              of AutomatedClusterDiscovery
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
